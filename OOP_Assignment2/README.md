# 객체지향프로그램 2차과제_주차장 관리 프로그램

## 개발일지
### 20년 11월 14일
+ 코드 변화 과정 및 기록을 남기기 위해 깃을 연동하였다
+ 날짜 계산을 하려는 과정에서 int[]로는 정확한 날짜 계산이 복잡해짐을 느끼고 구글링을 통해 Date, SimpleDateFormat을 찾았고 활용했다
+ 활용과정에서 기존 코드를 수정하며 compareTo와 toString에 추가적인 수정이 필요했고, compareTo는 Date.getTime(), toString은 SimpleDateFormat.format()을 활용하여 해결했다.
+ 그 결과 현재 입차시간과 출차시간의 차이로 값을 얻어왔고, 각 객체에 계산식에 따라 계산을 하여 주차요금을 받을 예정이다.

### 20년 11월 16일
+ 임플리먼트말고 추상메소드로 함. 어짜피 car는 생성 안하고 그냥 변수로만 써서 괜찮음.
+ 추상메소드로 Car에 시간계산 및 계산 메소드를 추가하고 각 객체에서 상속받아서 구현하여 Car 객체로 공통되게 사용함.
+ 주차시간계산이랑 돈계산 분리? -> 전기차때문에. 이미 지역변수로 선언해서 그냥 분리하는게 나을듯 어짜피
+ 주차시간 표기 h,m 지역변수로 해버리기
+ 그냥 메소드에서 값 빼고하니까 계산할때 값이 이미 줄어듬.

### 20년 11월 16일
+ standardtime 구할 떄, abstract를 쓰는 바람에 car에 있는 time이 private라서 쓰질 못함. 흠 어케 쓰지. 지금은 get으로 받아오는데
+ 메소드 분리하고 addincome, exit 메소드 완성.
+ 에러처리랑 전기차 계산 할 것.

### 20년 11월 17일
+ 전기차 충전 기능구현 완료
+ 예외처리 남음
- 처리해야할 유효성검사

	+ 잘못된 타입의 데이터가 입력되는 경우는 없다고 가정한다. 예를 들어, 정수를 필요로 하는 곳에 문자열 들어오는..
	+ 차량번호 4자리 정수 - ok
	+ 시간 제대로 작성하는지 - ok
	+ 입차시간이 출차시간보다 늦으면 오류이다. - ok
	+ 차량 종류 검사 - ok
	+ 출차 차량 번호가 주차 차량 목록에 없는 경우 - ok
	+ 메뉴 번호 범위 검사 - ok
- 에러처리를 다 올려서 test에서 다 처리하게 할까... 메세지로 어떤 에러인지 띄우고? 익셉션은 크게 어디 함수에서 나오는지만 몇개 만들고?
- 질문 : 사용자정의익셉션 만들때 어떤 익셉션을 부모로 쓰나. throw하고 throws 안했는데 호출한곳으로 간다. 가능한일?
	
### 20년 11월 18일
+ 조교님 답변을 받고 에러를 바꾸었다.	

### 20년 11월 19일
+ 에러 테스트에서 ㅇㅋ?
+ throws 공부
+ 지금 내 코드는 그냥 메인에서 쭈욱 써져있는 느낌?
+ 메뉴판 스태틱

### 20년 11월 21일
+ 완성

### 20년 12월 3일
- GUI 설계를 어느정도 잡아놨다.
- 기존 설계과 다르게 탑부터 바텀까지 삼등분하고 그 프레임을 상속받아서 사용하면 괜찮을 듯 하다.
- 오늘의 목표는 상속받는 부모 프레임 만들기
- setLocation으로 자동차 움직이게해서 시각적 요소 추가하는 것도 ㄱㅊ을듯.
- 하ㅏ아ㅏㅏㅏㅏㅏㅏㅏㅏㅏ개빡친다. html 최고 스윙 구대기 진짜 뭐 되는게 없어
- placeholder도 쓰고싶고, align 싹 다 다시하고 싶고 디자인 개구리다 진짜 ;;;;

### 20년 12월 5일
- setSize() 왜 안먹어!!!!!!!!!!!!!
- 디자인 미루고 일단 기능 구현중.
- 버튼이랑 엔터로 인풋 받는거 구현, 화면 전환 구현
- 원래 setvisable로 했다가 꺼졌다가 켜지는 느낌이 별로라. 화면에 그리고 지우는거로 함. 근데 프라이빗 패널을 겟하는 부분에서 캡슐이 꺠짐. 후...
- 질문 : 여러 클래스에 나눠져있는 프레임을 어떻게 지웠다 그렸다 할까.
- 캡슐화도 안깨고 무분별한 프레임 생성 및 visiable 사용을 막기위해, 메인 프레임에서 번호를 입력하면, 해당하는 기능의 프레임이 켜지고, 그 프레임 내부에선 패널을 새롭게 두어 패널을 지우고 그리는 방식으로 ㅇㅇ!
- insets : 컴포넌트 사이 공백, ipadx,y 컴포넌트 안에 패딩

- 다음부분 해야할거 엔트리 하면 거기서 어떻게 변화를 주고 리페인트 할지 정하기

### 20년 12월 7일
- 엔트리프레임, 화면 전환 및 리페인팅 완료
- 일단 지금부터 어떻게 기능을 이어나갈지 생각해야할듯

### 20년 12월 8일
- 기능 연결 성공.
- 익셉션 부분이 너무 지저분함. 전체적으로 코드를 깔끔하게 하고싶은데...
- 익셉션 트라이캐치로 묶음
- 초기화면 버튼 만들기
- 예외처리 완료, 초기화면 버튼 구현. 중복된 익셉션 처리 필요

### 20년 12월 10일
- 주차장 기능 구현 완료.
- 창전환, 에러뜨고 확인하면 포커싱 가있게 하기. -> 왜 안돼 아앙아오오오왜오애ㅗ애왱
- 자동차 우웅 애니미 넣기
- 쇼파킹랏 할 떄, 스크롤로 뜨게
- 익셉션 더 넣을거 있었음.

### 20년 12월 11일
- 포커싱 기능 구현 완료 - setvisible이 포커싱보다 앞서있게
- 할거 애니미, 익셉션, 4번 5번 기능
- 4,5번 기능 구현 완료